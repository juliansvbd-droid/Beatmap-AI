# BeatMap-AI

Read and follow `AGENTS.md` first: it explains the shared handoff system
(`docs/STATUS.md` = where the project stands, `docs/WORKLOG.md` = what the previous
agents did). Update both before you stop.
